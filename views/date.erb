<!DOCTYPE html>
<html>
    <head>
        <title>Sinatra Views Lab -- Date</title>
    </head>
    <body>
        <% today = DateTime.now.strftime("The date is %A, %B %d, %Y") %>
        <p><%= today %></p>
    </body>
</html>